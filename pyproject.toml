[project]
name = "iug-site"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
    "jinja2>=3.1",
    "python-frontmatter>=1.1",
    "mistune>=3.0",
    "pyyaml>=6.0",
    "watchdog>=4.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]

[dependency-groups]
dev = ["pytest>=8.0"]
